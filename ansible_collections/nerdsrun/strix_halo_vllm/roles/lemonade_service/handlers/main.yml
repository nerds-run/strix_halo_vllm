---
- name: Reload systemd user daemon
  ansible.builtin.systemd:
    daemon_reload: true
    scope: user
  become: false

- name: Restart lemonade service
  ansible.builtin.systemd:
    name: "{{ lemonade_container_name }}"
    state: restarted
    scope: user
  become: false
