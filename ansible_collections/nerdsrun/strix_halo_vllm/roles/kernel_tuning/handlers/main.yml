---
- name: Reboot for kernel args
  ansible.builtin.reboot:
    msg: "Rebooting to apply kernel boot parameter changes for Strix Halo."
    reboot_timeout: 600
    pre_reboot_delay: 5
    post_reboot_delay: 30
    test_command: uptime
  become: true
  when: strix_halo_kernel_reboot_allowed | bool
